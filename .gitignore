node_modules/
dist/
game/shots/
.uploads/

# Raw SWF rip: ~20MB of intermediate SVG, regenerated by ffdec. The game only
# needs game/public/assets, which tools/prep-assets.mjs derives from it.
extracted/

# Working sheets the art generator produced on the way to the finished sprites.
# The sprites themselves are in generated/ and are what tools/prep-crops.py cuts.
generated/_wip/

# secrets and the machine this happens to be running on
.env.local
.env
tools/.venv/
run/
# Points the build at whatever tunnel is up right now; not a fact about the game.
game/.env.production
